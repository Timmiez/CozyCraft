# Ignore directories that get generated by the game and are not needed in version control
.fabric/
data/
downloads/
dynamic-resource-pack-cache/
fancymenu_data/
logs/

# Ignore user specfic configuration files that update timestamps every starup
config/fabric/
config/iris.properties
config/jei/

# Ignore files that are just user-specific and not needed in version control
command_history.json
minecraftinstance.json
usercache.json
.mixin.out
.curseclient
saves/
shaderpacks/
screenshots/
notepad/